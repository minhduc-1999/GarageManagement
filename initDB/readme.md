Init DB:

1. Change `connectionString` in appsettings.json
2. Create an database `garadb`
3. Create an collection name `UserRoles` and import data from `UserRoles.csv`
4. Create an collection name `Users` and import data from `Users.csv`